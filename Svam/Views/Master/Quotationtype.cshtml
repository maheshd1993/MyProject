@{
    ViewBag.Title = "Quotation-type";
    Layout = "~/Views/Shared/_LayoutSiteMaster.cshtml";
}
<script src="/Scripts/SweetAlert/sweetalert.min.js"></script>
<link href="/Scripts/SweetAlert/sweetalert.css" rel="stylesheet" />
@{
    var al = TempData["success"];
    var alchk = TempData["alert"];
}
<script>

    var mas = '@al';
    var chkmsg = '@alchk';
    if (mas != '') {
        swal({ title: "Thank you!", text: mas, imageUrl: "/Scripts/SweetAlert/thumbs-up.jpg" });
    }
    if (chkmsg != '') {
        alert(chkmsg);
    }
</script>

@model Svam.Models.QuotationtypeModel
<div class="maincontainer">
    <div class="col-sm-12 ">
        <div class="col-sm-12">
            <div class="panel panel-default">
                <div class="panel-heading"><i class="fa fa-th-list" aria-hidden="true"></i>Manage Quotation Type</div>
                <div class="panel-body">

                    @using (Html.BeginForm("Quotationtype", "Master", FormMethod.Post))
                      {
                        <div class="form-group f-center col-sm-6 padd0">
                            <div class="col-sm-8">
                                  @Html.TextBoxFor(em => em.QuotationtypeName, new {@placeholder="Add Quotationtype Name",@class="form-control" })
                                  @Html.ValidationMessageFor(em=>em.QuotationtypeName)
                            </div>

                            <div class="col-sm-4">
                                <input type="submit" value="Submit" class="btn mainbtn">
                            </div>
                       </div>
                    }
                    <div class="devider"></div>

                    <div class="col-sm-12 padd0">
                        <div class="table-responsive">
                            <table class="table table-striped table table-hover tabledata table-bordered">
                                <thead>
                                    <tr>
                                        <th width="5%" class="noimg padd0" style="padding: 10px 0px;">Sr no.</th>
                                        <th width="70%">Quotation Type Name </th>
                                        <th width="12%">Status</th>
                                        <th width="10%" class="noimg">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @{int i = 1;}
                                    @foreach (var item in ViewBag.QuotationtypeList)
                                    {
                                        <tr>
                                            <td>@i</td>
                                            <td>@item.QuotationTypeName</td>
                                            <td>@item.Status</td>
                                            <td><a href="/Master/Quotationtype/@item.Id" class="viewbtn">Edit</a></td>
                                        </tr>
                                        i++;
                                    }
                                    
                                </tbody>
                            </table>

                            <div class="container paginationcustom">
                                <ul class="pagination">
                                </ul>
                            </div>
                        </div>

                    </div>
                </div>
            </div>

        </div>

    </div>




</div>
